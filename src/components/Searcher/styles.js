export const styles = theme => ({
  root: {
    width: "100%",
    marginTop: theme.spacing(3),
    overflowX: "auto"
  },
  table: {
    maxWidth: 100
  },
  searchField: {
    marginTop: theme.spacing(5)
  }
});
